[tool.poetry]
name = "data-structures-with-python"
version = "0.1.0"
description = ""
authors = ["Guilherme S. Magalhaes"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
